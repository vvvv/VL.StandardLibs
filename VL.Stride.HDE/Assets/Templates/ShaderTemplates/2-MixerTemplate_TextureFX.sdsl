///Combines the pixels of the two incoming textures.

[Category("Mixer")]
[Summary("")]
shader MixerTemplate_TextureFX : MixerBase
{
    float4 Mix(float4 tex0col, float4 tex1col, float fader)
    {
        return lerp(tex0col, tex1col, fader);
    }
};