///EXPERIMENTAL: Represents "a piece of code" that can be used to compose larger shaders. 
///This is the most flexible type of node, it can work together with all other shader node types.

[Summary("")]
shader ShaderFXTemplate_ShaderFX : ComputeFloat4
{
    float4 MyColor;

    override float4 Compute()
    {
        return MyColor;
    }
};