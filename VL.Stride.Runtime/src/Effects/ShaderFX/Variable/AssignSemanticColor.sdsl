shader AssignSemanticColor<MemberName VarName, Semantic NAME> : ComputeVoid, SemanticFloat4<VarName, NAME>
{
    compose ComputeColor Value;

    override void Compute()
    {
        streams.VarName = Value.Compute();
    }
};
