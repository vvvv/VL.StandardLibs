[Category("Filter")]
[Summary("Template for a TextureFX filter node")]
shader FilterTemplate_TextureFX : FilterBase
{
    [Color]
    float4 Color = float4(1, 1, 1, 1);

    float4 Filter(float4 tex0col)
    {
        return tex0col * Color;
    }
};
